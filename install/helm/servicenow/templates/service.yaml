apiVersion: v1
kind: Service
metadata:
  name: {{ include "servicenow.name" . }}
  labels:
    {{- include "servicenow.labels" . | nindent 4 }}
  annotations:
    {{- include "servicenow.argocd-syncwave" . | nindent 4 }}
spec:
  ports:
  - name: {{ .Values.service.portName }}
    port: {{ .Values.service.port }}
    protocol: TCP
    targetPort: {{ .Values.service.containerPort }}
  selector:
    {{- include "servicenow.selectorLabels" . | nindent 4 }}
